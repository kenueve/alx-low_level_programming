#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: program that prints single digit numbers from base 10
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%i\n", i);
	}
	return (0);
}
